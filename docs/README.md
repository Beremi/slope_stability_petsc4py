# Docs

Active docs:

- `config_case_matrix.md`
- `config_scheme_3d.md`

Historical notes from earlier investigation phases are kept here as reference material. Some of those notes may still mention the pre-reorganization `slope_stability/...` path layout; the current root layout is described in the top-level `README.md`.
