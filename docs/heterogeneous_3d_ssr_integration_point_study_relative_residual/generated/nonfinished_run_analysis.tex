The \texttt{P4/q45} run produced a monotone accepted branch through step 8, reaching \(\omega_{\mathrm{last}} = \num{6.52514e+06}\). The next continuation solve targeting \(\omega = \num{6.7e+06}\) exhausted the Newton cap of 200 iterations with terminal relative residual \num{0.00580949}. The continuation code then started a reduced-step retry, but that retry was intentionally terminated and is not promoted into the published branch. The report therefore freezes the accepted branch at the previous point and classifies the missing final step as a last-step maxit failure.
