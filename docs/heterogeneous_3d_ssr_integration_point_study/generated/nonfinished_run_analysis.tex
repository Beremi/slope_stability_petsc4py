The original study-configuration \texttt{P4/q45} run is different. It accepted nine continuation points and remained monotone in \(\omega\), but the ninth step terminated the run at \(\omega_{\mathrm{last}} = \num{6.63728e+06}\) because \texttt{scaled dlambda min} fired before the shared target \(\omega_{\mathrm{final}}\). This is a protocol stop, not an immediate linear-solver breakdown.

A diagnostic rerun of \texttt{P4/q45} with the scaled-\(\Delta\lambda\) continuation stop disabled shows that the branch can continue beyond the main-study cutoff. Under that relaxed stop policy the run reached \(\omega_{\mathrm{last}} = \num{6.7e+06}\) in \num{13106} s with stop reason \texttt{omega max stop}. This confirms that the original nonfinish was caused by the study-side continuation stop rather than by the solver failing at the same point.
