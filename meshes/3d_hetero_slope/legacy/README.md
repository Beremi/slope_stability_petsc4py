# Legacy Source

Legacy tagged Gmsh inputs for `3d_hetero_slope` live under `source/3d_hetero_ssr/` and `source/3d_hetero_ll/`.

Rerun the canonical retagging with `PYTHONPATH=src python meshes/3d_hetero_slope/legacy/retag_to_canonical.py`.
